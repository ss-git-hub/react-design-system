enum IconPosition {
	Left = 'left',
	Right = 'right',
}

enum ButtonType {
	Button = 'button',
	Submit = 'submit',
	Reset = 'reset',
}

type ButtonSize = 'sm' | 'md';
type ButtonVariant = 'contained' | 'outlined';
type ButtonColor = 'primary' | 'secondary';

export { IconPosition, ButtonType, ButtonSize, ButtonVariant, ButtonColor };
